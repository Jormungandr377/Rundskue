[providers]
node = "18"
python = "3.9"

[build]
cmd = "cd frontend && npm install && npm run build && cd .. && cd backend && pip install -r requirements.txt"

[start]
cmd = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
