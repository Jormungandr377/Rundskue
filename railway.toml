[build]
builder = "RAILPACK"
buildCommand = "apt-get update && apt-get install -y libpq-dev && bash build.sh"

[deploy]
startCommand = "python3 -m uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
restartPolicyType = "always"